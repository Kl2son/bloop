# Продакшен: пустой base URL → браузер ходит на тот же origin (Vercel).
# vercel.json проксирует /api/* на Render (обход блокировок onrender.com у части провайдеров).
# НЕ ставьте сюда https://….onrender.com — иначе клиент в РФ снова получит Failed to fetch.
VITE_BACKEND_URL=
VITE_API_URL=
